{
  "name": "court-recording-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Local agent that polls the Urban Padel booking API for recording jobs and records court cameras via ffmpeg.",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "qr": "node generate-qr.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
